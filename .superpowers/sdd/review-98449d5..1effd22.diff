# Review package: 98449d5..1effd22

## Commits
1effd22 fix: stabilize UI workspace tests

## Files changed
 packages/ui/src/styles/tokens.test.tsx | 5 +++--
 packages/ui/vitest.config.ts           | 1 +
 2 files changed, 4 insertions(+), 2 deletions(-)

## Diff
diff --git a/packages/ui/src/styles/tokens.test.tsx b/packages/ui/src/styles/tokens.test.tsx
index cc8775f..a45afcf 100644
--- a/packages/ui/src/styles/tokens.test.tsx
+++ b/packages/ui/src/styles/tokens.test.tsx
@@ -1,15 +1,16 @@
 import {readFileSync} from 'node:fs'
-import {resolve} from 'node:path'
+import {dirname, resolve} from 'node:path'
+import {fileURLToPath} from 'node:url'
 import {describe, expect, it} from 'vitest'
 
-const packageRoot = process.cwd()
+const packageRoot = resolve(dirname(fileURLToPath(import.meta.url)), '../..')
 const tokens = readFileSync(resolve(packageRoot, 'src/styles/tokens.css'), 'utf8')
 const packageManifest = JSON.parse(
   readFileSync(resolve(packageRoot, 'package.json'), 'utf8'),
 ) as {exports: Record<string, string>}
 
 describe('design token package contract', () => {
   it('defines semantic spacing and elevation tokens', () => {
     expect(tokens).toContain('--aifans-space-1:')
     expect(tokens).toContain('--aifans-space-4:')
     expect(tokens).toContain('--aifans-elevation-1:')
diff --git a/packages/ui/vitest.config.ts b/packages/ui/vitest.config.ts
index f276583..9cd23dc 100644
--- a/packages/ui/vitest.config.ts
+++ b/packages/ui/vitest.config.ts
@@ -1,8 +1,9 @@
 import {defineConfig} from 'vitest/config'
 
 export default defineConfig({
   test: {
+    environment: 'jsdom',
     include: ['src/**/*.test.tsx'],
     setupFiles: ['./src/test/setup.ts'],
   },
 })
