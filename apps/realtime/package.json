{
  "name": "@aifans/realtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:runtime": "node --test tests/workerd.test.mjs",
    "dev": "wrangler dev --env test",
    "check:bundle": "wrangler deploy --env test --dry-run"
  },
  "dependencies": {
    "@aifans/contracts": "workspace:*",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "wrangler": "4.129.0"
  }
}
