{
  "name": "aifans",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {"node": ">=24.19.0"},
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "pnpm license:check && turbo run lint",
    "license:check": "node scripts/check-forbidden-assets.mjs .",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "test:e2e": "playwright test --config=playwright.config.ts",
    "test:e2e:production": "WEB_API_RATE_LIMIT_SIGNING_SECRET=local_validation_secret_32_chars__ AIFANS_NEXT_DIST_DIR=.next-production-e2e pnpm -C apps/web build && WEB_API_RATE_LIMIT_SIGNING_SECRET=local_validation_secret_32_chars__ AIFANS_NEXT_DIST_DIR=.next-production-e2e playwright test --config=playwright.production.config.ts",
    "db:start": "docker compose -f infra/postgres/compose.yaml up -d --wait",
    "db:stop": "docker compose -f infra/postgres/compose.yaml down",
    "db:migrate": "pnpm --dir packages/db migrate",
    "db:test": "pnpm db:start && DATABASE_URL=postgresql://aifans_owner:local_only_aifans@127.0.0.1:55432/aifans_test pnpm db:migrate && DATABASE_URL=postgresql://aifans_owner:local_only_aifans@127.0.0.1:55432/aifans_test pnpm --dir packages/db test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^24.0.0",
    "prettier": "3.9.6",
    "turbo": "2.10.12",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
